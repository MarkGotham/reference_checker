# reference_checker — direct dependencies
# Python >= 3.9 required (uses built-in tuple[str, ...] type hints)
# Tested on Python 3.12.3
#
# Install: pip install -r requirements.txt

python >= 3.10         # On the safer side ;)
aiohttp==3.13.5        # async HTTP client for API queries
bibtexparser==1.4.4    # .bib file parsing
rapidfuzz==3.14.5      # fuzzy string matching for title/author/venue scoring
reportlab==4.4.10      # PDF report generation
tqdm==4.67.3           # progress bar (auto-suppressed when stdout is not a TTY)
